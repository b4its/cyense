"""Minimal model stubs so the sample package parses as valid Python."""


class Invoice:
    objects = None


class ReportFile:
    objects = None


class request:  # noqa: N801 (django-style name used in samples)
    GET: dict = {}
